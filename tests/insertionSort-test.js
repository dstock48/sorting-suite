import {expect, assert} from 'chai';
import insertionSort from '../scripts/insertionSort.js'
import genRanNum from '../scripts/genRanNum.js'

describe('Insertion Sort', () => {
  it('Should be a function', () => {
    assert.isFunction(insertionSort);
  })

  it.skip('Should sort an unsorted array of numbers', () => {

  })

  it.skip('Should sort large arrays', () => {

  })

  it.skip('Should move min number to front', () => {

  })
})
